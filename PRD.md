# 调拨申请网页项目 PRD

## 1. 文档信息

- 文档名称：调拨申请网页项目产品需求文档
- 文档格式：Markdown
- 文档语言：中文
- 项目类型：纯前端静态网页项目
- 技术范围：HTML / CSS / JavaScript

## 2. 项目背景

仓储调拨业务需要一个轻量、可快速交付的网页工具，用于：

1. 新建货品调拨申请单。
2. 浏览和检索历史调拨申请单。
3. 以低成本方式完成基础交互验证、页面演示和流程确认。

当前项目为静态原型，不接入真实后端数据库，不做登录鉴权，不做持久化存储。列表数据与 SKU 数据均由前端本地模拟。

## 3. 项目目标

### 3.1 业务目标

1. 提供调拨申请录入入口，支持仓库间货品调拨单的创建。
2. 提供调拨申请列表页，支持按关键信息筛选与查看详情。
3. 明确调拨申请创建页与调拨申请管理页之间的导航关系。

### 3.2 产品目标

1. 页面结构清晰，支持桌面端与移动端基础适配。
2. 核心交互全部在浏览器端完成，无网络依赖。
3. 表单校验明确，错误提示可见。
4. 侧边栏导航可收起/展开，提升页面可用空间。

## 4. 用户角色

### 4.1 仓储/运营人员

负责创建调拨申请、查看调拨列表、定位某一条申请记录。

### 4.2 管理/审核相关人员

通过列表页检索申请单，并查看单据详情进行后续业务处理。

## 5. 页面范围

本项目包含以下两个核心页面：

1. `Goods Allocation Request`
   用于新建调拨申请。
2. `List of Allocation Request`
   用于查询、管理和查看调拨申请列表。

## 6. 信息架构

### 6.1 左侧导航

左侧侧边栏属于全局导航，默认展示，支持收起/展开。

- 一级菜单：`Allocation Management`
- 二级菜单：
  - `List of Allocation Request`
  - `Goods Allocation Request`

### 6.2 侧边栏规则

1. 侧边栏默认展开。
2. 侧边栏右上角提供切换按钮。
3. 点击切换按钮后，侧边栏整体收起或展开。
4. 收起时主内容区域自动扩展填充剩余空间。
5. 侧边栏高度需始终至少为 `100vh`，并尽量与浏览器可视区高度一致。

## 7. 页面一：Goods Allocation Request

## 7.1 页面目标

用于录入一张新的调拨申请单，并完成行项目维护与前端校验。

## 7.2 页面结构

页面由以下模块组成：

1. 左侧导航栏
2. 页面标题区
3. 申请头信息区
4. 明细行区
5. SKU 选择弹窗

## 7.3 头信息字段

| 字段名 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| Creation Date | 文本，只读 | 是 | 默认显示浏览器本地当前日期，用户不可编辑 |
| Execution Date | 日期 | 否 | 计划执行日期 |
| Remark | 文本输入 | 否 | 备注信息 |
| From Warehouse | 下拉 + 自定义输入 | 是 | 来源仓库 |
| To Warehouse | 下拉 + 自定义输入 | 是 | 目标仓库 |

## 7.4 仓库字段交互

`From Warehouse` 与 `To Warehouse` 均采用“预置下拉 + 自定义输入”的模式。

### 规则

1. 默认展示下拉框。
2. 下拉中包含若干预置仓库选项及 `Custom...`。
3. 当选择 `Custom...` 时，显示并启用对应文本输入框。
4. 当切换回预置仓库时，自定义输入框隐藏并清空。
5. 提交时取最终解析值：
   - 若选择预置值，则取预置值。
   - 若选择 `Custom...`，则取自定义输入值。

## 7.5 明细区字段

每条明细行包含以下字段：

| 字段名 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| SKU Number | 只读文本 / 选择器入口 | 是 | 点击后打开 SKU 选择弹窗 |
| SKU Name | 文本输入 | 否 | 选中 SKU 后自动带出，可人工调整 |
| Batch Number | 文本输入 | 是 | 选中 SKU 后自动带出 |
| Goods Status | 下拉 + 自定义输入 | 否 | 货品状态 |
| Quantity | 数字输入 | 是 | 正整数 |
| Remove Row | 图标按钮 | 否 | 删除当前明细行 |

## 7.6 明细区交互

### 7.6.1 新增明细行

1. 页面初始默认生成 1 条空明细行。
2. 点击 `+` 按钮新增一条空明细行。

### 7.6.2 删除明细行

1. 每条明细行在 `Quantity` 字段右侧提供删除图标。
2. 点击后立即删除当前行。
3. 允许删除到 0 行，但提交时必须至少存在 1 条有效明细。

### 7.6.3 Goods Status 交互

1. 下拉框包含预置值：`New`、`Damaged`、`Expired`、`Other...`
2. 当选择 `Other...` 时，展示自定义输入框。
3. 提交时按最终解析值输出。

## 7.7 SKU 选择弹窗

### 7.7.1 入口

点击明细行中的 `SKU Number` 字段，打开 SKU 选择弹窗。

### 7.7.2 弹窗内容

弹窗包含：

1. 关闭按钮
2. SKU 过滤输入框
3. SKU 列表表格

### 7.7.3 SKU 列表字段

| 字段名 | 说明 |
| --- | --- |
| SKU Number | SKU 编号 |
| SKU Name | SKU 名称 |
| Batch Number | 批次号 |
| Quantity In Stock | 库存数量 |

### 7.7.4 SKU 过滤

支持按以下内容模糊过滤：

1. SKU Number
2. SKU Name
3. Batch Number
4. Quantity In Stock

### 7.7.5 选择后回填

用户选择某一条 SKU 后，自动回填到当前明细行：

1. `SKU Number`
2. `SKU Name`
3. `Batch Number`

并关闭弹窗。

## 7.8 表单校验规则

提交时执行前端校验。

### 7.8.1 头信息校验

1. `From Warehouse` 必填
2. `To Warehouse` 必填

### 7.8.2 明细行校验

一条明细要被视为有效，必须满足：

1. `SKU Number` 非空
2. `Batch Number` 非空
3. `Quantity` 为正整数

### 7.8.3 明细数量校验

1. 提交时至少存在 1 条明细行
2. 提交时至少存在 1 条有效明细数据

### 7.8.4 错误提示

1. 错误信息显示在字段附近
2. 无效字段显示错误样式
3. 用户修改字段后，对应错误提示应可被清除

## 7.9 提交行为

当前版本点击 `Submit` 后：

1. 阻止默认提交行为
2. 执行表单校验
3. 校验通过后，不调用后端接口
4. 当前实现只完成前端通过验证，不做真实数据保存

## 7.10 创建页数据结构

创建页内部围绕如下数据结构构建：

```json
{
  "creation_date": "YYYY-MM-DD",
  "execution_date": "YYYY-MM-DD",
  "Remark": "string",
  "From_warehouse": "string",
  "To_warehouse": "string",
  "details": [
    {
      "SKU_number": "string",
      "SKU_name": "string",
      "batch_number": "string",
      "goods_status": "string",
      "quantity": 1
    }
  ]
}
```

## 8. 页面二：List of Allocation Request

## 8.1 页面目标

用于管理调拨申请列表，支持搜索、重置、查看详情以及跳转到新增页面。

## 8.2 页面结构

页面由以下模块组成：

1. 左侧导航栏
2. 页面标题区
3. Searching Panel
4. Action Panel
5. Request Information Panel
6. 详情弹窗

## 8.3 Searching Panel

### 8.3.1 搜索字段

| 字段名 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| Request Order Number | 文本输入 | 否 | 按申请单号搜索 |
| Submitter | 文本输入 | 否 | 按提交人搜索 |
| Request Order Status | 下拉 | 否 | 状态枚举筛选 |
| SKU Number | 文本输入 | 否 | 按 SKU 编号搜索 |

### 8.3.2 状态枚举

`Request Order Status` 包含以下值：

1. `Approving`
2. `delivering`
3. `stock pending`
4. `finished`

### 8.3.3 搜索按钮

点击 `Search` 后：

1. 读取当前搜索条件
2. 对前端本地模拟数据执行过滤
3. 刷新下方列表

### 8.3.4 重置按钮

点击 `Reset` 后：

1. 清空所有搜索输入项
2. 恢复展示全部列表数据

## 8.4 Action Panel

包含一个主操作按钮：

| 按钮 | 行为 |
| --- | --- |
| Add | 跳转至 `Goods Allocation Request` 页面 |

## 8.5 Request Information Panel

以表格形式展示调拨申请列表。

### 8.5.1 表格字段

| 列名 | 说明 |
| --- | --- |
| Number of Allocation Request | 调拨申请单号 |
| From Warehouse | 来源仓 |
| To Warehouse | 目标仓 |
| Total of Allocation Quantity | 所有明细数量汇总 |
| Submitter | 提交人 |
| Creation Date | 创建日期 |
| Request Order Status | 申请状态 |
| Action | 操作列 |

### 8.5.2 编号规则

调拨申请单号遵循如下规则：

`DB` + `YYYYMMDD` + `####`

示例：

- `DB202604240001`
- `DB202604240002`

### 8.5.3 横向滚动与冻结列

1. 表格支持横向滚动。
2. 最后一列 `Action` 固定在右侧。
3. 第一列当前版本不再固定。

## 8.6 列表筛选逻辑

### 8.6.1 Request Order Number

按单号做包含匹配。

### 8.6.2 Submitter

按提交人做包含匹配。

### 8.6.3 Request Order Status

按状态做精确匹配。

### 8.6.4 SKU Number

若某申请单关联的 SKU 列表中任一 SKU 命中，则视为匹配。

## 8.7 Action 列

每行展示一个高亮链接：`Details`

点击后打开申请详情弹窗。

## 8.8 详情弹窗

### 8.8.1 弹窗展示字段

| 字段名 | 说明 |
| --- | --- |
| Request Order Number | 调拨申请单号 |
| Submitter | 提交人 |
| From Warehouse | 来源仓 |
| To Warehouse | 目标仓 |
| Creation Date | 创建日期 |
| Execution Date | 执行日期 |
| Total Allocation Quantity | 总调拨数量 |
| Request Order Status | 当前状态 |
| SKU Numbers | 关联 SKU 列表 |
| Remark | 备注 |

### 8.8.2 关闭方式

支持以下关闭方式：

1. 点击关闭按钮
2. 点击遮罩层
3. 键盘按下 `Escape`

## 9. 本地模拟数据

当前项目不接入真实数据库，使用前端本地数组模拟：

1. SKU 主数据
2. 调拨申请列表数据

该方式仅用于：

1. 演示页面结构
2. 演示交互流程
3. 演示筛选与弹窗逻辑

不代表最终生产态数据来源。

## 10. 非功能需求

## 10.1 技术约束

1. 必须使用纯 HTML / CSS / JavaScript
2. 不引入后端服务
3. 不依赖数据库连接
4. 不依赖前端框架

## 10.2 兼容与适配

1. 页面需支持桌面端与移动端基本可用
2. 栅格布局应在窄屏下自动换行
3. 弹窗、表格、侧边栏在不同宽度下不应出现功能性失效

## 10.3 交互性能

1. 所有交互应在前端即时响应
2. 搜索与重置不应触发页面刷新
3. 侧边栏收起/展开需要有平滑过渡

## 10.4 可维护性

1. 样式集中在 `styles.css`
2. 创建页逻辑集中在 `app.js`
3. 列表页逻辑集中在 `list.js`
4. 页面文件与脚本文件边界清晰

## 11. 版本边界

## 11.1 当前版本已包含

1. 调拨申请创建页
2. 调拨申请列表页
3. 左侧导航栏及收起功能
4. SKU 选择弹窗
5. 列表详情弹窗
6. 本地模拟数据搜索与展示
7. 前端表单校验

## 11.2 当前版本不包含

1. 真实后端接口
2. 真实数据库读写
3. 用户登录/权限控制
4. 审批流程流转
5. 文件导入导出
6. 提交后持久化保存
7. 审计日志
8. 国际化

## 12. 后续建议

建议后续版本按以下顺序推进：

1. 接入真实调拨申请新增接口
2. 接入真实列表查询接口
3. 接入真实 SKU 主数据接口
4. 增加申请状态流转与审批动作
5. 增加详情页或详情抽屉的扩展信息
6. 增加列表分页、排序与导出能力

## 13. 验收标准

### 13.1 创建页验收

1. 页面打开后默认显示一条空明细行。
2. `Creation Date` 自动显示当天日期且不可编辑。
3. 来源仓和目标仓支持预置值与自定义输入切换。
4. 点击 `SKU Number` 可打开 SKU 选择弹窗。
5. 选择某条 SKU 后，当前行自动回填 SKU 编号、名称和批次号。
6. 点击新增按钮可继续增加明细行。
7. 点击删除图标可删除明细行。
8. 明细为 0 行或无有效行时，不允许提交。
9. `Quantity` 非正整数时显示错误。
10. `SKU Number` 或 `Batch Number` 为空时显示错误。

### 13.2 列表页验收

1. 搜索条件支持按单号、提交人、状态、SKU 编号过滤。
2. 点击 `Search` 后列表按条件刷新。
3. 点击 `Reset` 后条件清空且列表恢复默认数据。
4. 点击 `Add` 可打开创建页。
5. 表格展示完整列信息。
6. 表格横向滚动时，`Action` 列始终可见。
7. 点击 `Details` 可打开详情弹窗。
8. 详情弹窗可通过关闭按钮、遮罩层或 `Escape` 关闭。
9. 侧边栏默认展示。
10. 点击侧边栏右上角按钮后，侧边栏可收起/展开，主内容区域同步扩展/回收。

